<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/navigation') %>
    <main class="main-content container">
        <div class="card bg-dark text-white text-center">
            <div class="card-header">
                <h3><%= title %></h3>
            </div>
            <div class="card-body text-start">
                <form action="<%= isEditMode ? '/submit-form/' + id : '/submit-form' %>" method="post">
                    <% if (isEditMode) { %>
                        <input type="hidden" name="_method" value="PUT">
                    <% } %>

                    <div class="mb-3">
                        <label for="location">Location</label>
                        <input type="text" name="location" placeholder="location" value="<%= location %>" class="form-control">    
                    </div>

                    <div class="mb-3">
                        <label for="firstName">Firstname</label>
                        <input type="text" name="firstName" placeholder="first name" value="<%= firstName %>" class="form-control">    
                    </div>

                    <div class="mb-3">
                        <label for="lastName">Lastname</label>
                        <input type="text" name="lastName" placeholder="last name" value="<%= lastName %>" class="form-control">    
                    </div>

                    <div class="mb-3">
                        <label for="age">Age</label>
                        <input type="number" name="age" placeholder="age" value="<%= isEditMode ? age : null %>" class="form-control">    
                    </div>

                    <div class="mb-3">
                        <label for="description">Description</label>
                        <input type="text" name="description" placeholder="eg 'granny'" value="<%= description %>" class="form-control">    
                    </div>
                
                    <div class="d-flex justify-content-end">
                        <% if (isEditMode) { %>
                            <a class="btn btn-primary btn-block" href="/local-news/<%= id %>">
                                See It
                            </a>
                        <% } %>

                        <button type="submit" class="btn btn-secondary btn-block ms-auto">
                            <%= isEditMode ? 'Update' : 'Save' %>
                        </button>
                    </div>
                    
                </form>
            </div>
        </div>
    </main>
    <%- include('partials/footer') %>
</body>
</html>